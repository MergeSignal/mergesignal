/**
 * Public-repo expectations for @mergesignal/contracts (GitHub Packages).
 * Bump only after publishing contracts-vX.Y.Z from mergesignal-engine — keep in sync with
 * pnpm-workspace.yaml catalog entry.
 */
export const EXPECTED_CONTRACTS_PACKAGE_VERSION = "0.12.0" as const;
