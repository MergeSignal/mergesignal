{
  "name": "@mergesignal/product-tier",
  "version": "0.1.0",
  "private": true,
  "description": "Product owner tier and reasoning-budget mapping for hosted API and worker",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
